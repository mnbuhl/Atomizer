# Requirements Archive: v1.0 — Atomizer Storage Refactor

**Archived:** 2026-05-03
**Status:** ✅ SHIPPED — all active requirements satisfied

---

## v1 Requirements — Final Status

### Leasing Abstraction

- [x] **LEASE-01**: Storage consumers call a single `ExecuteInLeaseAsync(queue, callback)` method — the open transaction/lock is managed inside the callback, not by the caller
- [x] **LEASE-02**: `IAtomizerLeasingScopeFactory` and `IAtomizerLeasingScope` are removed from the public API; `Acquired` flag is gone — a lease either succeeds or throws/returns empty
- [x] **LEASE-03**: The leasing contract does not assume SQL transactions — each backend decides how to implement atomicity (transaction, SemaphoreSlim, distributed lock, optimistic retry)

### Atomic Acquisition

- [x] **ACQR-01**: EF Core `GetDueJobsAsync` acquires rows using `FOR UPDATE SKIP LOCKED` (PostgreSQL/MySQL) or `WITH (UPDLOCK, ROWLOCK, READPAST)` (SQL Server) inside the active lease transaction
- [x] **ACQR-02**: EF Core `GetDueSchedulesAsync` uses the same provider-appropriate row-locking inside the lease transaction
- [x] **ACQR-03**: Row locks are held until `UpdateJobsAsync` / `UpdateSchedulesAsync` commits — double-dispatch is structurally impossible within a single lease callback

### Schedule Upsert

- [x] **UPSRT-01**: `UpsertScheduleAsync` for PostgreSQL uses `INSERT ... ON CONFLICT (job_key) DO UPDATE SET ...` — atomic, no race condition
- [x] **UPSRT-02**: `UpsertScheduleAsync` for SQL Server uses `MERGE ... USING ... WHEN MATCHED THEN UPDATE WHEN NOT MATCHED THEN INSERT`
- [x] **UPSRT-03**: `UpsertScheduleAsync` for MySQL uses `INSERT ... ON DUPLICATE KEY UPDATE ...`
- [x] **UPSRT-04**: The existing `@todo` race condition (check-then-insert) is eliminated across all supported providers

### SQL Dialect Strategy

- [x] **DIAL-01**: A `ISqlDialect` interface (or equivalent) is introduced — exposes methods for due-job acquisition SQL and schedule upsert SQL
- [x] **DIAL-02**: `PostgreSqlDialect`, `SqlServerDialect`, and `MySqlDialect` implement `ISqlDialect` — each encapsulates all provider-specific SQL for the storage layer
- [x] **DIAL-03**: EF Core storage class contains no inline provider-branching SQL — it delegates all raw SQL to the injected dialect
- [x] **DIAL-04**: Adding a new relational provider requires only implementing `ISqlDialect` and registering it — no changes to the storage class itself

### InMemory Alignment

- [x] **INMEM-01**: InMemory backend implements the same callback-based leasing contract as EF Core — callers cannot observe behavioral differences
- [x] **INMEM-02**: InMemory `GetDueJobsAsync` holds its per-queue `SemaphoreSlim` lock for the duration of the lease callback (released after the callback completes, same as EF Core transaction lifecycle)
- [x] **INMEM-03**: InMemory `UpsertScheduleAsync` is atomic — uses the existing lock to prevent the same race condition as the SQL @todo

### Public API & Versioning

- [x] **COMPAT-01**: `IAtomizerStorage` interface is updated to reflect the new leasing contract — this is a documented breaking change
- ~~**COMPAT-02**: A major version bump is applied to both `Atomizer` and `Atomizer.EntityFrameworkCore` NuGet packages~~ — **DROPPED 2026-05-03**: project will not publish to NuGet this milestone
- [x] **COMPAT-03**: XML documentation on all new/changed public API members

---

## Traceability — Final Status

| Requirement | Phase | Final Status | Notes |
|-------------|-------|-------------|-------|
| LEASE-01 | Phase 1 | ✅ Satisfied | Both pollers use `ExecuteInLeaseAsync` |
| LEASE-02 | Phase 1 | ✅ Satisfied | All 4 deprecated types deleted from public API |
| LEASE-03 | Phase 1 | ✅ Satisfied | Interface has no SQL/transaction primitives |
| COMPAT-01 | Phase 1 | ✅ Satisfied | Breaking change documented; interface updated |
| INMEM-01 | Phase 2 | ✅ Satisfied | Same callback contract; behavioral parity |
| INMEM-02 | Phase 2 | ✅ Satisfied | SemaphoreSlim held for full callback duration |
| INMEM-03 | Phase 2 | ✅ Satisfied | Scheduler semaphore guards UpsertScheduleAsync |
| DIAL-01 | Phase 3 | ✅ Satisfied | `ISqlDialect` internal interface, 4 methods |
| DIAL-02 | Phase 3 | ✅ Satisfied | 3 dialect implementations; 12 tests pass |
| DIAL-03 | Phase 3 | ✅ Satisfied | Zero inline provider branching in storage |
| DIAL-04 | Phase 3 | ✅ Satisfied | New provider = 1 ISqlDialect impl + 1 case in CreateDialect |
| ACQR-01 | Phase 4 | ✅ Satisfied | FOR UPDATE SKIP LOCKED / WITH (UPDLOCK) in all 3 dialects |
| ACQR-02 | Phase 4 | ✅ Satisfied | Schedule acquisition uses same row-locking |
| ACQR-03 | Phase 4 | ✅ Satisfied | ReadCommitted transaction spans GetDueJobs + UpdateJobs |
| UPSRT-01 | Phase 4 | ✅ Satisfied | ON CONFLICT ... DO UPDATE (PostgreSQL) |
| UPSRT-02 | Phase 4 | ✅ Satisfied | MERGE ... WITH (HOLDLOCK) (SQL Server) |
| UPSRT-03 | Phase 4 | ✅ Satisfied | INSERT ... ON DUPLICATE KEY UPDATE (MySQL) |
| UPSRT-04 | Phase 4 | ✅ Satisfied | @todo race eliminated across all 3 providers |
| COMPAT-02 | Phase 5 | ❌ Dropped | No NuGet publish this milestone |
| COMPAT-03 | Phase 5 | ✅ Satisfied | GenerateDocumentationFile=true; zero CS1591 |

---

*Archived: 2026-05-03*
*Next milestone requirements will be defined via `/gsd-new-milestone`*
