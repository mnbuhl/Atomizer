---
phase: 05-cleanup-and-versioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScope.cs
  - src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScopeFactory.cs
  - src/Atomizer/Atomizer.csproj
  - src/Atomizer.EntityFrameworkCore/Atomizer.EntityFrameworkCore.csproj
autonomous: true
requirements:
  - COMPAT-03

must_haves:
  truths:
    - "DatabaseTransactionLeasingScope is declared internal sealed — not visible outside the assembly"
    - "DatabaseTransactionLeasingScopeFactory.cs does not exist"
    - "Both csproj files produce an XML documentation file on build"
  artifacts:
    - path: "src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScope.cs"
      provides: "internal sealed leasing scope (access modifier changed)"
      contains: "internal sealed class DatabaseTransactionLeasingScope"
    - path: "src/Atomizer/Atomizer.csproj"
      provides: "doc file generation enabled"
      contains: "GenerateDocumentationFile"
    - path: "src/Atomizer.EntityFrameworkCore/Atomizer.EntityFrameworkCore.csproj"
      provides: "doc file generation enabled"
      contains: "GenerateDocumentationFile"
  key_links:
    - from: "EntityFrameworkCoreStorage.cs"
      to: "DatabaseTransactionLeasingScope.StartTransaction"
      via: "internal call — same assembly, no visibility change needed"
      pattern: "DatabaseTransactionLeasingScope\\.StartTransaction"
---

<objective>
Make `DatabaseTransactionLeasingScope` internal sealed (per D-01), delete the dead
`DatabaseTransactionLeasingScopeFactory` (per D-02), and add `<GenerateDocumentationFile>true`
to both csproj files (per D-03). These are mechanical one-liner changes that unblock the
CS1591-driven documentation work in plans 02 and 03.

Note on COMPAT-02: The version bump requirement is deferred per CONTEXT.md decision —
no published adopters; breaking changes are acceptable without a semver major bump at this stage.
COMPAT-03 (XML documentation) is the active requirement addressed across plans 01-04.

Purpose: Close the last public-visibility leak in the EF Core package and enable the build
enforcement that makes missing XML docs a compile error.
Output: Modified DatabaseTransactionLeasingScope.cs, deleted factory file, updated csproj files.
</objective>

<execution_context>
@/Users/mbuhl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbuhl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mbuhl/projects/Atomizer/.planning/PROJECT.md
@/Users/mbuhl/projects/Atomizer/.planning/ROADMAP.md
@/Users/mbuhl/projects/Atomizer/.planning/STATE.md

<interfaces>
<!-- Key signatures the executor needs — no codebase exploration required. -->

From src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScope.cs (current, line 10):
```csharp
public class DatabaseTransactionLeasingScope : IDisposable, IAsyncDisposable
```

Target state (line 10):
```csharp
internal sealed class DatabaseTransactionLeasingScope : IDisposable, IAsyncDisposable
```

The existing XML doc comment on the class (lines 7-9) must be preserved exactly as-is.
All constructors, fields, properties, and methods stay byte-for-byte identical.

From src/Atomizer/Atomizer.csproj — first PropertyGroup ends at line 12, add after line 11 (EnablePackageValidation):
```xml
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
```

From src/Atomizer.EntityFrameworkCore/Atomizer.EntityFrameworkCore.csproj — first PropertyGroup ends at line 12, add after line 11 (ImplicitUsings):
```xml
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make DatabaseTransactionLeasingScope internal sealed and delete the factory</name>
  <read_first>
    - src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScope.cs (current access modifier and class declaration on line 10)
    - src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScopeFactory.cs (confirm no callers reference it before deleting)
    - src/Atomizer.EntityFrameworkCore/Storage/EntityFrameworkCoreStorage.cs (confirm it calls DatabaseTransactionLeasingScope.StartTransaction directly, not via the factory)
  </read_first>
  <files>
    src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScope.cs,
    src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScopeFactory.cs
  </files>
  <action>
    1) In DatabaseTransactionLeasingScope.cs, change exactly line 10 from:
       `public class DatabaseTransactionLeasingScope : IDisposable, IAsyncDisposable`
       to:
       `internal sealed class DatabaseTransactionLeasingScope : IDisposable, IAsyncDisposable`
       The existing `/// <summary>Wraps a database transaction...</summary>` doc comment on lines 7-9 stays untouched.
       All other lines of the file (constructor, Acquired property, Abort, Dispose, DisposeAsync, StartTransaction) remain byte-for-byte identical.

    2) Delete src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScopeFactory.cs entirely.
       Justification (per D-02): zero callers, no DI registration. EntityFrameworkCoreStorage.cs
       calls DatabaseTransactionLeasingScope.StartTransaction directly (not via the factory).
  </action>
  <verify>
    <automated>grep -n "internal sealed class DatabaseTransactionLeasingScope" /Users/mbuhl/projects/Atomizer/src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScope.cs</automated>
    <automated>test ! -f /Users/mbuhl/projects/Atomizer/src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScopeFactory.cs && echo "factory deleted" || echo "factory still exists"</automated>
  </verify>
  <acceptance_criteria>
    - grep returns: `internal sealed class DatabaseTransactionLeasingScope : IDisposable, IAsyncDisposable`
    - DatabaseTransactionLeasingScopeFactory.cs does not exist on disk
    - The existing summary doc comment on DatabaseTransactionLeasingScope (lines 7-9 of the original file) is present and unchanged
    - All methods (Abort, Dispose, DisposeAsync, StartTransaction) are present and unchanged
  </acceptance_criteria>
  <done>DatabaseTransactionLeasingScope is internal sealed; factory file is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Add GenerateDocumentationFile to both csproj files</name>
  <read_first>
    - src/Atomizer/Atomizer.csproj (identify exact location to insert — inside first PropertyGroup after EnablePackageValidation on line 11)
    - src/Atomizer.EntityFrameworkCore/Atomizer.EntityFrameworkCore.csproj (identify exact location — inside first PropertyGroup after ImplicitUsings on line 11)
  </read_first>
  <files>
    src/Atomizer/Atomizer.csproj,
    src/Atomizer.EntityFrameworkCore/Atomizer.EntityFrameworkCore.csproj
  </files>
  <action>
    Per D-03: Add `<GenerateDocumentationFile>true</GenerateDocumentationFile>` to the first
    `<PropertyGroup>` in each csproj file. Do NOT create a new PropertyGroup.

    In src/Atomizer/Atomizer.csproj — insert immediately after the `<EnablePackageValidation>true</EnablePackageValidation>` line (line 11), still inside the first `<PropertyGroup>`:
    ```xml
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
    ```

    In src/Atomizer.EntityFrameworkCore/Atomizer.EntityFrameworkCore.csproj — insert immediately after the `<ImplicitUsings>enable</ImplicitUsings>` line (line 11), still inside the first `<PropertyGroup>`:
    ```xml
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
    ```

    Use 4-space indentation consistent with the rest of the file. No other changes.
  </action>
  <verify>
    <automated>grep "GenerateDocumentationFile" /Users/mbuhl/projects/Atomizer/src/Atomizer/Atomizer.csproj /Users/mbuhl/projects/Atomizer/src/Atomizer.EntityFrameworkCore/Atomizer.EntityFrameworkCore.csproj</automated>
  </verify>
  <acceptance_criteria>
    - Both files contain `<GenerateDocumentationFile>true</GenerateDocumentationFile>`
    - The entry appears inside the first `<PropertyGroup>` block (not in a second or new PropertyGroup)
    - No other lines in either file were changed
  </acceptance_criteria>
  <done>Both csproj files have GenerateDocumentationFile enabled; CS1591 will now be a build error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build system | csproj changes affect what is compiled and published |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | DatabaseTransactionLeasingScope (previously public) | mitigate | Making it `internal sealed` removes it from the public API surface; external consumers can no longer reference the type |
| T-05-02 | Tampering | csproj GenerateDocumentationFile | accept | Build-time setting only; no runtime attack surface |
</threat_model>

<verification>
After both tasks complete:
```bash
cd /Users/mbuhl/projects/Atomizer
grep "internal sealed class DatabaseTransactionLeasingScope" src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScope.cs
test ! -f src/Atomizer.EntityFrameworkCore/Storage/DatabaseTransactionLeasingScopeFactory.cs
grep "GenerateDocumentationFile" src/Atomizer/Atomizer.csproj src/Atomizer.EntityFrameworkCore/Atomizer.EntityFrameworkCore.csproj
```
</verification>

<success_criteria>
- DatabaseTransactionLeasingScope.cs line 10 reads `internal sealed class DatabaseTransactionLeasingScope : IDisposable, IAsyncDisposable`
- DatabaseTransactionLeasingScopeFactory.cs is absent from disk
- Both csproj files contain `<GenerateDocumentationFile>true</GenerateDocumentationFile>` inside their first PropertyGroup
- No other files changed by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/05-cleanup-and-versioning/05-01-SUMMARY.md`
</output>
