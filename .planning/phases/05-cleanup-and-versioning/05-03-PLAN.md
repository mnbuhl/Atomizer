---
phase: 05-cleanup-and-versioning
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/Atomizer/Models/Base/Model.cs
  - src/Atomizer/Models/Base/ValueObject.cs
  - src/Atomizer/Models/AtomizerJob.cs
  - src/Atomizer/Models/AtomizerSchedule.cs
  - src/Atomizer/Models/AtomizerJobError.cs
  - src/Atomizer/Models/ValueObjects/QueueKey.cs
  - src/Atomizer/Models/ValueObjects/JobKey.cs
  - src/Atomizer/Models/ValueObjects/LeaseToken.cs
  - src/Atomizer/Models/ValueObjects/RetryStrategy.cs
  - src/Atomizer/Models/ValueObjects/Schedule.cs
  - src/Atomizer/Exceptions/InvalidQueueKeyException.cs
  - src/Atomizer/Exceptions/InvalidJobKeyException.cs
  - src/Atomizer/Exceptions/InvalidLeaseTokenException.cs
  - src/Atomizer/Exceptions/InvalidRetryStrategyException.cs
  - src/Atomizer/Exceptions/InvalidAtomizerConfigurationException.cs
  - src/Atomizer/Exceptions/JobResolverException.cs
  - src/Atomizer/Exceptions/PayloadSerializationException.cs
autonomous: true
requirements:
  - COMPAT-03

must_haves:
  truths:
    - "AtomizerJob, AtomizerSchedule, and AtomizerJobError have XML docs on class, all public properties, static Create factory, and all domain methods"
    - "Model and ValueObject base classes have XML docs on class and all public members"
    - "QueueKey, JobKey, LeaseToken, RetryStrategy, and Schedule value objects have XML docs on class, constructor, and all public members"
    - "All 7 exception types have XML docs on class and all constructors"
    - "AtomizerJobStatus and MisfirePolicy enums have XML docs on enum type and each member"
  artifacts:
    - path: "src/Atomizer/Models/AtomizerJob.cs"
      provides: "XML docs on class + enum + all properties + Create + domain methods"
      contains: "/// <summary>"
    - path: "src/Atomizer/Models/ValueObjects/RetryStrategy.cs"
      provides: "XML docs on class + Default/None + Fixed/Intervals/Exponential + ShouldRetry/GetRetryInterval"
      contains: "/// <summary>"
    - path: "src/Atomizer/Exceptions/InvalidQueueKeyException.cs"
      provides: "XML docs on class and all constructors"
      contains: "/// <summary>"
  key_links:
    - from: "AtomizerJob.cs"
      to: "IAtomizerStorage.cs doc style"
      via: "gold-standard pattern — one sentence summary, active voice param, Task<T> returns"
      pattern: "/// <summary>"
---

<objective>
Add missing XML documentation to all public domain model classes, value objects, and exception
types. This is the largest documentation batch — AtomizerJob alone has ~15 members requiring docs.

Purpose: Every public member in these 17 files must carry `<summary>` (and `<param>`/`<returns>`
where applicable) so that `dotnet build` produces zero CS1591 errors for the model and exceptions layer.
Output: 17 files with complete XML documentation conforming to the gold-standard style in IAtomizerStorage.cs.
</objective>

<execution_context>
@/Users/mbuhl/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbuhl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/mbuhl/projects/Atomizer/.planning/PROJECT.md
@/Users/mbuhl/projects/Atomizer/.planning/ROADMAP.md
@/Users/mbuhl/projects/Atomizer/.planning/STATE.md

<interfaces>
<!-- Gold-standard doc style from IAtomizerStorage.cs. Follow exactly for all new docs. -->

XML doc rules (same as plan 02):
- `<summary>` on every public type and member. One prose sentence ending with a period.
- `<param name="x">` for every parameter. Starts with capital letter, ends with period.
- `<returns>` for non-void returns.
- `<typeparam name="T">` for generic type parameters.
- `<remarks>` nested INSIDE `<summary>` block.
- No `<exception>` tags.
- `cancellationToken` for all storage/domain APIs.
- `<see cref="TypeName"/>` when referencing another type inline.
- Enum members: one-line `/// <summary>Text.</summary>` on the line immediately before the member.

Current gaps per file (members needing docs):

Model.cs — EVERYTHING undocumented:
  - abstract class Model
  - Id property

ValueObject.cs — EVERYTHING undocumented:
  - abstract class ValueObject : IEquatable<ValueObject>
  - GetEqualityValues() protected abstract method
  - Equals(object?) override
  - Equals(ValueObject?) IEquatable implementation
  - GetHashCode() override
  - == operator
  - != operator

AtomizerJob.cs — EVERYTHING undocumented:
  - class AtomizerJob
  - QueueKey property
  - PayloadType property
  - Payload property
  - ScheduledAt property
  - VisibleAt property
  - Status property
  - Attempts property
  - RetryStrategy property
  - CreatedAt property
  - UpdatedAt property
  - CompletedAt property
  - FailedAt property
  - LeaseToken property
  - ScheduleJobKey property
  - IdempotencyKey property
  - Errors property
  - static Create(...)
  - Lease(...)
  - Release(...)
  - Attempt()
  - MarkAsCompleted(...)
  - MarkAsFailed(...)
  - Reschedule(...)
  - enum AtomizerJobStatus
  - Pending, Processing, Completed, Failed enum members

AtomizerSchedule.cs — EVERYTHING undocumented:
  - class AtomizerSchedule
  - JobKey property
  - QueueKey property
  - PayloadType property
  - Payload property
  - Schedule property
  - TimeZone property
  - MisfirePolicy property
  - MaxCatchUp property
  - Enabled property
  - RetryStrategy property
  - NextRunAt property
  - LastEnqueueAt property
  - CreatedAt property
  - UpdatedAt property
  - static Create(...)
  - GetOccurrences(...)
  - UpdateNextOccurence(...)
  - Disable(...)
  - enum MisfirePolicy
  - Ignore, ExecuteNow, CatchUp enum members

AtomizerJobError.cs — EVERYTHING undocumented:
  - class AtomizerJobError
  - JobId property
  - ErrorMessage property
  - StackTrace property
  - ExceptionType property
  - CreatedAt property
  - Attempt property
  - RuntimeIdentity property
  - static Create(...)

QueueKey.cs — gaps:
  - class QueueKey (line 6)
  - Default static field (line 8) — internal Scheduler field does NOT need docs
  - constructor QueueKey(string key) (line 11)
  - Key property (line 26)
  - implicit operator string (line 28)
  - implicit operator QueueKey (line 30)
  - ToString() (line 32)
  - GetEqualityValues() (line 34)

JobKey.cs — gaps:
  - class JobKey (line 8)
  - constructor JobKey(string key) (line 8)
  - Key property (line 22)
  - implicit operator string (line 25)
  - implicit operator JobKey (line 27)
  - ToString() (line 25)
  - GetEqualityValues() (line 31)

LeaseToken.cs — gaps:
  - class LeaseToken (line 6)
  - Token property (line 8)
  - InstanceId property (line 10)
  - QueueKey property (line 11)
  - LeaseId property (line 12)
  - constructor LeaseToken(string token) (line 14)
  - GetEqualityValues() (line 38)
  - ToString() (line 43)
  - implicit operator string (line 48)
  - implicit operator LeaseToken (line 53)

RetryStrategy.cs — gaps:
  - class RetryStrategy (line 6)
  - MaxAttempts property (line 8)
  - RetryIntervals property (line 9)
  - Default static property (line 13)
  - None static property (line 15)
  - Fixed(...) static method (line 17)
  - Intervals(...) static method (line 39)
  - Exponential(...) static method (line 56)
  - ShouldRetry(...) (line 108)
  - GetRetryInterval(...) (line 113)
  - GetEqualityValues() (line 123)

Schedule.cs — gaps:
  - class Schedule (line 5)
  - Seconds property (line 7)
  - Minutes property (line 8)
  - Hours property (line 9)
  - DayOfMonth property (line 10)
  - Month property (line 11)
  - DayOfWeek property (line 12)
  - Default static property (line 24)
  - EverySecond static property (line 25)
  - EveryMinute static property (line 26)
  - Hourly static property (line 27)
  - Daily static property (line 28)
  - Weekly static property (line 29)
  - Monthly static property (line 30)
  - Cron(string) static method (line 32)
  - ToString() (line 44)
  - GetEqualityValues() (line 46)

All 7 exception files — EVERYTHING undocumented (class + all constructors).

Exact doc text to use:

Model class: "Base class for all Atomizer persistent entities, providing a unique identifier."
Model.Id: "Gets or sets the unique identifier for this entity."

ValueObject class: "Base class for Atomizer value objects that define equality by their component values."
GetEqualityValues: "Returns the sequence of values that define equality for this value object." + returns "An enumerable of the equality-defining component values."
Equals(object?): "Determines whether the specified object is equal to this value object."  (+ param obj "The object to compare." + returns "true if equal; otherwise false.")
Equals(ValueObject?): /// <inheritdoc/>
GetHashCode: "Returns a hash code derived from the equality component values."
== operator: "Returns true if both value objects are equal or both are null."  (+ param left + param right + returns)
!= operator: "Returns true if the two value objects are not equal."  (+ param left + param right + returns)

AtomizerJob class: "Represents a single enqueued or scheduled job in the Atomizer system."
QueueKey prop: "Gets or sets the queue this job belongs to."
PayloadType prop: "Gets or sets the CLR type of the serialized payload."
Payload prop: "Gets or sets the serialized payload string."
ScheduledAt prop: "Gets or sets the earliest UTC time at which this job may be processed."
VisibleAt prop: "Gets or sets the UTC time after which this job becomes visible to workers again."
Status prop: "Gets or sets the current lifecycle state of this job."
Attempts prop: "Gets or sets the number of processing attempts made so far."
RetryStrategy prop: "Gets or sets the retry strategy applied when this job fails."
CreatedAt prop: "Gets or sets the UTC time this job was created."
UpdatedAt prop: "Gets or sets the UTC time this job was last modified."
CompletedAt prop: "Gets or sets the UTC time this job completed successfully, or null if not yet completed."
FailedAt prop: "Gets or sets the UTC time this job was permanently failed, or null if not yet failed."
LeaseToken prop: "Gets or sets the lease token held by the worker currently processing this job, or null if not leased."
ScheduleJobKey prop: "Gets or sets the key of the recurring schedule that produced this job, or null for ad-hoc jobs."
IdempotencyKey prop: "Gets or sets the deduplication key used to prevent duplicate job insertions."
Errors prop: "Gets or sets the list of error records from previous failed attempts."

AtomizerJob.Create:
/// <summary>
/// Creates a new <see cref="AtomizerJob"/> in the <see cref="AtomizerJobStatus.Pending"/> state.
/// </summary>
/// <param name="queueKey">The queue to place the job in.</param>
/// <param name="payloadType">The CLR type of the serialized payload.</param>
/// <param name="payload">The serialized payload string.</param>
/// <param name="createdAt">The UTC time the job was created.</param>
/// <param name="scheduledAt">The earliest UTC time the job may be processed.</param>
/// <param name="retryStrategy">Optional retry strategy; defaults to <see cref="RetryStrategy.Default"/>.</param>
/// <param name="idempotencyKey">Optional key used to deduplicate identical jobs.</param>
/// <param name="scheduleJobKey">Optional key linking this job to a recurring schedule.</param>
/// <returns>A new <see cref="AtomizerJob"/> instance.</returns>

AtomizerJob.Lease:
/// <summary>
/// Transitions the job from <see cref="AtomizerJobStatus.Pending"/> to <see cref="AtomizerJobStatus.Processing"/>.
/// </summary>
/// <param name="leaseToken">The lease token identifying the owning worker.</param>
/// <param name="now">The current UTC time.</param>
/// <param name="visibilityTimeout">How long the job remains invisible to other workers.</param>

AtomizerJob.Release:
/// <summary>
/// Returns the job to <see cref="AtomizerJobStatus.Pending"/> and clears the lease, making it available for reprocessing.
/// </summary>
/// <param name="now">The current UTC time.</param>

AtomizerJob.Attempt:
/// <summary>
/// Increments the attempt counter. Must be called while the job is in <see cref="AtomizerJobStatus.Processing"/>.
/// </summary>

AtomizerJob.MarkAsCompleted:
/// <summary>
/// Transitions the job to <see cref="AtomizerJobStatus.Completed"/> and records the completion time.
/// </summary>
/// <param name="completedAt">The UTC time the job completed.</param>

AtomizerJob.MarkAsFailed:
/// <summary>
/// Transitions the job to <see cref="AtomizerJobStatus.Failed"/> and records the failure time.
/// </summary>
/// <param name="failedAt">The UTC time the job was permanently failed.</param>

AtomizerJob.Reschedule:
/// <summary>
/// Returns the job to <see cref="AtomizerJobStatus.Pending"/> with a future visibility time for retry.
/// </summary>
/// <param name="nextVisibleAt">The UTC time at which the job becomes visible again.</param>
/// <param name="now">The current UTC time used to set <see cref="AtomizerJob.UpdatedAt"/>.</param>

AtomizerJobStatus enum: "Represents the lifecycle state of an <see cref="AtomizerJob"/>."
Pending: "The job is waiting to be picked up for processing."
Processing: "The job has been leased and is actively being processed."
Completed: "The job handler completed successfully."
Failed: "The job exhausted all retry attempts and will not be retried."

AtomizerSchedule class: "Represents a recurring schedule definition in the Atomizer system."
JobKey prop: "Gets or sets the unique key identifying this recurring schedule."
QueueKey prop: "Gets or sets the queue into which occurrences of this schedule are enqueued."
PayloadType prop: "Gets or sets the CLR type of the serialized payload."
Payload prop: "Gets or sets the serialized payload string used for each job occurrence."
Schedule prop: "Gets or sets the cron-based schedule defining when occurrences run."
TimeZone prop: "Gets or sets the time zone in which the cron expression is evaluated."
MisfirePolicy prop: "Gets or sets the policy controlling behavior when a scheduled run is missed."
MaxCatchUp prop: "Gets or sets the maximum number of missed runs to catch up on when using <see cref=\"MisfirePolicy.CatchUp\"/>."
Enabled prop: "Gets or sets whether this schedule is active and will be polled."
RetryStrategy prop: "Gets or sets the retry strategy applied when a job occurrence fails."
NextRunAt prop: "Gets or sets the UTC time of the next scheduled occurrence."
LastEnqueueAt prop: "Gets or sets the UTC time the most recent occurrence was enqueued, or null if never enqueued."
CreatedAt prop: "Gets or sets the UTC time this schedule was created."
UpdatedAt prop: "Gets or sets the UTC time this schedule was last modified."

AtomizerSchedule.Create:
/// <summary>
/// Creates a new <see cref="AtomizerSchedule"/> with the initial next run time computed from the cron expression.
/// </summary>
/// <param name="jobKey">The unique key identifying this schedule.</param>
/// <param name="queueKey">The queue into which occurrences are enqueued.</param>
/// <param name="payloadType">The CLR type of the serialized payload.</param>
/// <param name="payload">The serialized payload string for each occurrence.</param>
/// <param name="schedule">The cron-based schedule expression.</param>
/// <param name="timeZone">The time zone for cron evaluation.</param>
/// <param name="createdAt">The UTC time the schedule was created.</param>
/// <param name="misfirePolicy">Policy for handling missed runs. Defaults to <see cref="MisfirePolicy.ExecuteNow"/>.</param>
/// <param name="maxCatchUp">Maximum missed runs to catch up. Defaults to 5.</param>
/// <param name="enabled">Whether the schedule is active. Defaults to true.</param>
/// <param name="retryStrategy">Optional retry strategy; defaults to <see cref="RetryStrategy.Default"/>.</param>
/// <returns>A new <see cref="AtomizerSchedule"/> instance.</returns>

AtomizerSchedule.GetOccurrences:
/// <summary>
/// Returns the list of job occurrences that should be enqueued at or before <paramref name="now"/>,
/// applying the configured <see cref="AtomizerSchedule.MisfirePolicy"/>.
/// </summary>
/// <param name="now">The current UTC time.</param>
/// <returns>An ordered list of UTC occurrence timestamps to enqueue.</returns>

AtomizerSchedule.UpdateNextOccurence:
/// <summary>
/// Advances <see cref="AtomizerSchedule.NextRunAt"/> to the next cron occurrence after <paramref name="horizon"/>.
/// </summary>
/// <param name="horizon">The UTC time from which to compute the next occurrence.</param>
/// <param name="now">The current UTC time used to set <see cref="AtomizerSchedule.UpdatedAt"/>.</param>

AtomizerSchedule.Disable:
/// <summary>
/// Disables this schedule so it is no longer polled.
/// </summary>
/// <param name="now">The current UTC time used to set <see cref="AtomizerSchedule.UpdatedAt"/>.</param>

MisfirePolicy enum: "Controls how the scheduler handles a schedule whose NextRunAt was missed."
Ignore: "Skip the missed run and advance to the next scheduled occurrence."
ExecuteNow: "Enqueue one job immediately for the missed occurrence, then advance."
CatchUp: "Enqueue one job per missed occurrence, up to <see cref=\"AtomizerSchedule.MaxCatchUp\"/>."

AtomizerJobError class: "Records a single failed attempt for an <see cref="AtomizerJob"/>."
JobId prop: "Gets or sets the identifier of the job that failed."
ErrorMessage prop: "Gets or sets the exception message from the failed attempt, or null if unavailable."
StackTrace prop: "Gets or sets the stack trace from the failed attempt, truncated to 5,120 characters."
ExceptionType prop: "Gets or sets the fully qualified exception type name, or null if unavailable."
CreatedAt prop: "Gets or sets the UTC time this error record was created."
Attempt prop: "Gets or sets the attempt number on which this error occurred."
RuntimeIdentity prop: "Gets or sets the instance identifier of the worker that made this attempt."

AtomizerJobError.Create:
/// <summary>
/// Creates a new <see cref="AtomizerJobError"/> capturing details from an exception.
/// </summary>
/// <param name="jobId">The identifier of the job that failed.</param>
/// <param name="createdAt">The UTC time the error was recorded.</param>
/// <param name="attempt">The attempt number on which the error occurred.</param>
/// <param name="exception">The exception that caused the failure, or <see langword="null"/> if unavailable.</param>
/// <param name="runtimeIdentity">The instance identifier of the worker that attempted the job.</param>
/// <returns>A new <see cref="AtomizerJobError"/> instance.</returns>

QueueKey class: "Identifies a job queue by name. Maximum length is 100 characters."
QueueKey.Default: "The default queue key used when no queue is specified."
QueueKey constructor: summary "Initializes a new <see cref="QueueKey"/> with the specified name." + param key "The queue name. Must be non-empty and at most 100 characters."
QueueKey.Key: "Gets the queue name."
QueueKey implicit→string: "Implicitly converts a <see cref="QueueKey"/> to its string representation."  (+ param queueKey + returns "The queue name string.")
QueueKey implicit←string: "Implicitly converts a string to a <see cref="QueueKey"/>."  (+ param name + returns "A new <see cref="QueueKey"/> wrapping the string.")
QueueKey.ToString: "Returns the queue name."
QueueKey.GetEqualityValues: "Returns the queue name as the sole equality component."  (+ returns "An enumerable containing the queue name.")

JobKey class: "Identifies a recurring schedule by name. Maximum length is 255 characters."
JobKey constructor: summary "Initializes a new <see cref="JobKey"/> with the specified name." + param key "The schedule name. Must be non-empty and at most 255 characters."
JobKey.Key: "Gets the schedule name."
JobKey implicit→string: "Implicitly converts a <see cref="JobKey"/> to its string representation."  (+ param jobKey + returns "The schedule name string.")
JobKey implicit←string: "Implicitly converts a string to a <see cref="JobKey"/>."  (+ param key + returns "A new <see cref="JobKey"/> wrapping the string.")
JobKey.ToString: "Returns the schedule name."
JobKey.GetEqualityValues: "Returns the schedule name as the sole equality component."  (+ returns "An enumerable containing the schedule name.")

LeaseToken class: "Identifies a batch of leased jobs held by a specific worker instance on a specific queue. Format: {InstanceId}:*:{QueueKey}:*:{LeaseId}."
LeaseToken.Token: "Gets the raw token string."
LeaseToken.InstanceId: "Gets the worker instance identifier component of the token."
LeaseToken.QueueKey: "Gets the queue key component of the token."
LeaseToken.LeaseId: "Gets the unique lease identifier component of the token."
LeaseToken constructor: summary "Initializes a new <see cref="LeaseToken"/> by parsing the raw token string." + param token "The raw token string in format '{InstanceId}:*:{QueueKey}:*:{LeaseId}'."
LeaseToken.GetEqualityValues: "Returns the raw token string as the sole equality component."  (+ returns "An enumerable containing the token string.")
LeaseToken.ToString: "Returns the raw token string."
LeaseToken implicit→string: "Implicitly converts a <see cref="LeaseToken"/> to its raw string representation."  (+ param leaseToken + returns "The raw token string.")
LeaseToken implicit←string: "Implicitly converts a string to a <see cref="LeaseToken"/>."  (+ param token + returns "A new <see cref="LeaseToken"/> parsed from the string.")

RetryStrategy class: "Defines the retry behavior for a failed job, including the number of attempts and per-attempt delays."
MaxAttempts prop: "Gets the maximum number of attempts before the job is marked as failed."
RetryIntervals prop: "Gets the delay to apply before each retry attempt."
Default: "Gets the default retry strategy: 3 attempts with 15-second fixed delays and plus-or-minus 20% jitter."
None: "Gets a strategy that makes a single attempt with no retries."
Fixed: full docs — see interfaces block above
Intervals: full docs — see interfaces block above
Exponential: full docs — see interfaces block above
ShouldRetry: "Returns true if another attempt should be made after the given attempt number." + param attempt "The zero-based attempt count already made." + returns "true when attempt is less than MaxAttempts."
GetRetryInterval: "Returns the delay to apply before the specified attempt." + param attempt "The one-based attempt number (1 = first retry)." + returns "The TimeSpan delay for the given attempt."
GetEqualityValues: "Returns the MaxAttempts and all retry intervals as equality components." + returns "An enumerable of equality-defining values."

Schedule class: "Represents a 6-part cron schedule (seconds-level precision) used for recurring job definitions."
Seconds: "Gets the seconds field of the cron expression."
Minutes: "Gets the minutes field of the cron expression."
Hours: "Gets the hours field of the cron expression."
DayOfMonth: "Gets the day-of-month field of the cron expression."
Month: "Gets the month field of the cron expression."
DayOfWeek: "Gets the day-of-week field of the cron expression."
Default: "Gets a schedule equivalent to every second (* * * * * *)."
EverySecond: "Gets a schedule that fires every second."
EveryMinute: "Gets a schedule that fires at the start of every minute."
Hourly: "Gets a schedule that fires at the top of every hour."
Daily: "Gets a schedule that fires at midnight UTC every day."
Weekly: "Gets a schedule that fires at midnight UTC every day (same as Daily — use Cron for specific days)."
Monthly: "Gets a schedule that fires at midnight UTC on a recurring monthly basis."
Cron: "Creates a <see cref="Schedule"/> from a 5- or 6-part cron expression string." + param cronExpression "A standard 5-part or seconds-extended 6-part cron expression." + returns "A new <see cref="Schedule"/> parsed from the expression."
Schedule.ToString: "Returns the 6-part cron expression string."
Schedule.GetEqualityValues: "Returns all six cron fields as equality components." + returns "An enumerable of the six cron field strings."

Exception classes (same pattern for all ArgumentException-derived):
class summary = "Thrown when a <see cref="Xxx"/> is constructed with an invalid value." (replace Xxx with the relevant type)
  - InvalidQueueKeyException → "Thrown when a <see cref="QueueKey"/> is constructed with an invalid value."
  - InvalidJobKeyException → "Thrown when a <see cref="JobKey"/> is constructed with an invalid value."
  - InvalidLeaseTokenException → "Thrown when a <see cref="LeaseToken"/> is constructed with an invalid value."
  - InvalidRetryStrategyException → "Thrown when a <see cref="RetryStrategy"/> is constructed with invalid parameters."

Constructor docs for ArgumentException-derived (4 overloads each):
  ctor(string message): summary "Initializes a new instance with the specified message." + param message "The error message."
  ctor(string message, Exception): summary "Initializes a new instance with the specified message and inner exception." + param message "The error message." + param innerException "The exception that caused this exception."
  ctor(string message, string paramName): summary "Initializes a new instance with the specified message and parameter name." + param message "The error message." + param paramName "The name of the parameter that caused the exception."
  ctor(string message, string paramName, Exception): summary "Initializes a new instance with the specified message, parameter name, and inner exception." + param message "The error message." + param paramName "The name of the parameter that caused the exception." + param innerException "The exception that caused this exception."

Exception-derived classes (no paramName overloads):
  - InvalidAtomizerConfigurationException → "Thrown when the Atomizer configuration is invalid or incomplete."
  - JobResolverException → "Thrown when the job handler for a payload type cannot be resolved from the service container."
  - PayloadSerializationException → "Thrown when a job payload cannot be serialized or deserialized."

Constructor docs for Exception-derived (2-3 overloads):
  InvalidAtomizerConfigurationException:
    ctor(string message): "Initializes a new instance with the specified message."
    ctor(string message, Exception): "Initializes a new instance with the specified message and inner exception."

  JobResolverException:
    ctor(string message): "Initializes a new instance with the specified message."
    ctor(string message, Exception): "Initializes a new instance with the specified message and inner exception."
    ctor(string message, Type payloadType): summary "Initializes a new instance for the specified payload type, embedding the type name in the message." + param message "Additional detail about the resolution failure." + param payloadType "The payload type whose handler could not be resolved."

  PayloadSerializationException:
    ctor(string message): "Initializes a new instance with the specified message."
    ctor(string message, Exception): "Initializes a new instance with the specified message and inner exception."
    ctor(string message, Type payloadType, bool deserialization): summary "Initializes a new instance for the specified payload type, embedding the type name and direction in the message." + param message "Additional detail about the failure." + param payloadType "The payload type that failed to serialize or deserialize." + param deserialization "When true, the failure was during deserialization; otherwise serialization."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add XML docs to base classes and domain models (Model, ValueObject, AtomizerJob, AtomizerSchedule, AtomizerJobError)</name>
  <read_first>
    - src/Atomizer/Models/Base/Model.cs (2 members to doc: class + Id property)
    - src/Atomizer/Models/Base/ValueObject.cs (class + 6 members to doc)
    - src/Atomizer/Models/AtomizerJob.cs (class + 17 properties + Create + 6 domain methods + enum + 4 enum members)
    - src/Atomizer/Models/AtomizerSchedule.cs (class + 14 properties + Create + 3 domain methods + enum + 3 enum members)
    - src/Atomizer/Models/AtomizerJobError.cs (class + 7 properties + Create)
    - src/Atomizer/Abstractions/IAtomizerStorage.cs (gold-standard doc style — read for pattern only, do not modify)
  </read_first>
  <files>
    src/Atomizer/Models/Base/Model.cs,
    src/Atomizer/Models/Base/ValueObject.cs,
    src/Atomizer/Models/AtomizerJob.cs,
    src/Atomizer/Models/AtomizerSchedule.cs,
    src/Atomizer/Models/AtomizerJobError.cs
  </files>
  <action>
    Add XML doc comments using the exact text provided in the interfaces block above for each member.

    Model.cs:
    - Add class summary above `public abstract class Model`.
    - Add Id property summary above `public Guid Id { get; set; }`.

    ValueObject.cs:
    - Add class summary above `public abstract class ValueObject : IEquatable<ValueObject>`.
    - Add GetEqualityValues docs (summary + returns) above `protected abstract IEnumerable<object> GetEqualityValues()`.
    - Add Equals(object?) docs (summary + param + returns) above `public override bool Equals(object? obj)`.
    - For `public bool Equals(ValueObject? other)` — use `/// <inheritdoc/>`.
    - Add GetHashCode docs (summary) above `public override int GetHashCode()`.
    - Add == operator docs (summary + 2 params + returns) above `public static bool operator ==(`.
    - Add != operator docs (summary + 2 params + returns) above `public static bool operator !=(`.

    AtomizerJob.cs:
    - Add class summary above `public class AtomizerJob : Model`.
    - Add property summaries for all 17 properties (QueueKey through Errors) using exact text above.
    - Add full Create docs (summary + 8 params + returns) above `public static AtomizerJob Create(`.
    - Add Lease docs (summary + 3 params) above `public void Lease(`.
    - Add Release docs (summary + 1 param) above `public void Release(`.
    - Add Attempt docs (summary only, no params/returns for void no-param) above `public void Attempt()`.
    - Add MarkAsCompleted docs (summary + param) above `public void MarkAsCompleted(`.
    - Add MarkAsFailed docs (summary + param) above `public void MarkAsFailed(`.
    - Add Reschedule docs (summary + 2 params) above `public void Reschedule(`.
    - Add AtomizerJobStatus enum summary above `public enum AtomizerJobStatus`.
    - Add one-line `/// <summary>Text.</summary>` for each enum member (Pending, Processing, Completed, Failed).

    AtomizerSchedule.cs:
    - Add class summary above `public class AtomizerSchedule : Model`.
    - Add property summaries for all 14 properties using exact text above.
    - Add full Create docs (summary + 11 params + returns) above `public static AtomizerSchedule Create(`.
    - Add GetOccurrences docs (summary + param + returns) above `public List<DateTimeOffset> GetOccurrences(`.
    - Add UpdateNextOccurence docs (summary + 2 params) above `public void UpdateNextOccurence(`.
    - Add Disable docs (summary + param) above `public void Disable(`.
    - Add MisfirePolicy enum summary above `public enum MisfirePolicy`.
    - Add one-line summaries for each enum member (Ignore, ExecuteNow, CatchUp).
    - Note: the inline comments `// skip this run; advance to next` etc. on enum members can be kept or removed — add the `/// <summary>` XML doc on the line BEFORE the enum member.

    AtomizerJobError.cs:
    - Add class summary above `public class AtomizerJobError : Model`.
    - Add property summaries for all 7 properties using exact text above.
    - Add full Create docs (summary + 5 params + returns) above `public static AtomizerJobError Create(`.
  </action>
  <verify>
    <automated>grep -c "/// <summary>" /Users/mbuhl/projects/Atomizer/src/Atomizer/Models/Base/Model.cs /Users/mbuhl/projects/Atomizer/src/Atomizer/Models/Base/ValueObject.cs /Users/mbuhl/projects/Atomizer/src/Atomizer/Models/AtomizerJob.cs /Users/mbuhl/projects/Atomizer/src/Atomizer/Models/AtomizerSchedule.cs /Users/mbuhl/projects/Atomizer/src/Atomizer/Models/AtomizerJobError.cs</automated>
  </verify>
  <acceptance_criteria>
    - Model.cs: 2 summary tags (class + Id)
    - ValueObject.cs: at least 6 summary/inheritdoc tags covering class + 6 members
    - AtomizerJob.cs: at least 28 summary tags (class + 17 props + Create + 6 methods + enum + 4 members)
    - AtomizerSchedule.cs: at least 22 summary tags (class + 14 props + Create + 3 methods + enum + 3 members)
    - AtomizerJobError.cs: at least 10 summary tags (class + 7 props + Create)
    - AtomizerJobStatus enum members: each member has a `/// <summary>` doc
    - MisfirePolicy enum members: each member has a `/// <summary>` doc
    - No existing code logic changed
  </acceptance_criteria>
  <done>Base classes and domain models fully documented.</done>
</task>

<task type="auto">
  <name>Task 2: Add XML docs to value objects (QueueKey, JobKey, LeaseToken, RetryStrategy, Schedule) and all 7 exception types</name>
  <read_first>
    - src/Atomizer/Models/ValueObjects/QueueKey.cs (class + 7 members to doc; internal Scheduler field skipped)
    - src/Atomizer/Models/ValueObjects/JobKey.cs (class + 7 members to doc)
    - src/Atomizer/Models/ValueObjects/LeaseToken.cs (class + 10 members to doc)
    - src/Atomizer/Models/ValueObjects/RetryStrategy.cs (class + 10 members to doc including 3 factory methods)
    - src/Atomizer/Models/ValueObjects/Schedule.cs (class + 16 members to doc)
    - src/Atomizer/Exceptions/InvalidQueueKeyException.cs (class + 4 constructors)
    - src/Atomizer/Exceptions/InvalidJobKeyException.cs (class + 4 constructors)
    - src/Atomizer/Exceptions/InvalidLeaseTokenException.cs (class + 4 constructors)
    - src/Atomizer/Exceptions/InvalidRetryStrategyException.cs (class + 4 constructors)
    - src/Atomizer/Exceptions/InvalidAtomizerConfigurationException.cs (class + 2 constructors)
    - src/Atomizer/Exceptions/JobResolverException.cs (class + 3 constructors)
    - src/Atomizer/Exceptions/PayloadSerializationException.cs (class + 3 constructors)
  </read_first>
  <files>
    src/Atomizer/Models/ValueObjects/QueueKey.cs,
    src/Atomizer/Models/ValueObjects/JobKey.cs,
    src/Atomizer/Models/ValueObjects/LeaseToken.cs,
    src/Atomizer/Models/ValueObjects/RetryStrategy.cs,
    src/Atomizer/Models/ValueObjects/Schedule.cs,
    src/Atomizer/Exceptions/InvalidQueueKeyException.cs,
    src/Atomizer/Exceptions/InvalidJobKeyException.cs,
    src/Atomizer/Exceptions/InvalidLeaseTokenException.cs,
    src/Atomizer/Exceptions/InvalidRetryStrategyException.cs,
    src/Atomizer/Exceptions/InvalidAtomizerConfigurationException.cs,
    src/Atomizer/Exceptions/JobResolverException.cs,
    src/Atomizer/Exceptions/PayloadSerializationException.cs
  </files>
  <action>
    Add XML doc comments using the exact text in the interfaces block above for each member.
    The internal `Scheduler` field in QueueKey.cs does NOT need docs — skip it.
    WorkerId.cs is `internal sealed` — skip it entirely, not in scope.

    Value objects — for each file, add:
    - Class summary above the class declaration.
    - Static field/property summaries (Default, None, EverySecond, etc.) using one-line format.
    - Constructor docs (summary + all params).
    - Instance property summaries.
    - All public method docs including implicit operators, ToString, and GetEqualityValues.
    - For GetEqualityValues — it is `protected override`, not `public`, but it appears in the gap list
      because ValueObject's abstract declaration is public. Add a `/// <inheritdoc/>` on the override
      implementations in QueueKey, JobKey, LeaseToken, Schedule. For RetryStrategy's GetEqualityValues,
      add the full doc since it has a non-trivial implementation worth describing.

    Exception files — for each file, add:
    - Class summary above the class declaration.
    - Constructor docs for every constructor (summary + params).
    - Follow exactly the overload-specific texts listed in the interfaces block above.

    Important for RetryStrategy.Fixed, Intervals, Exponential:
    Use the full multi-line doc text provided in the interfaces block above for these three static methods.
    Each method has summary + multiple params + returns.
  </action>
  <verify>
    <automated>grep -c "/// <summary>" /Users/mbuhl/projects/Atomizer/src/Atomizer/Models/ValueObjects/RetryStrategy.cs /Users/mbuhl/projects/Atomizer/src/Atomizer/Models/ValueObjects/Schedule.cs /Users/mbuhl/projects/Atomizer/src/Atomizer/Exceptions/InvalidQueueKeyException.cs /Users/mbuhl/projects/Atomizer/src/Atomizer/Exceptions/JobResolverException.cs</automated>
  </verify>
  <acceptance_criteria>
    - QueueKey.cs: class summary + Default summary + constructor doc + Key doc + 2 implicit operator docs + ToString doc
    - JobKey.cs: same pattern as QueueKey
    - LeaseToken.cs: class summary + 4 property summaries + constructor doc + 2 implicit operator docs + ToString doc
    - RetryStrategy.cs: class summary + MaxAttempts + RetryIntervals + Default + None + Fixed (full multi-param) + Intervals (full) + Exponential (full) + ShouldRetry + GetRetryInterval — all documented
    - Schedule.cs: class summary + 6 field props + 7 static props + Cron method + ToString doc
    - All 4 ArgumentException-derived exceptions: class summary + all 4 constructor overloads documented
    - InvalidAtomizerConfigurationException: class summary + 2 constructors documented
    - JobResolverException: class summary + all 3 constructors documented (including Type payloadType overload)
    - PayloadSerializationException: class summary + all 3 constructors documented (including Type + bool overload)
    - internal Scheduler field in QueueKey.cs has no doc added
    - WorkerId.cs not touched
    - No existing code logic changed in any file
  </acceptance_criteria>
  <done>All 12 value object and exception files fully documented with zero CS1591 gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API surface | XML docs are part of the published NuGet API contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | XML doc content on exceptions | accept | Exception docs describe observable behavior; no internal implementation details or secrets exposed |
</threat_model>

<verification>
After both tasks complete, spot-check doc presence:
```bash
grep "/// <summary>" \
  /Users/mbuhl/projects/Atomizer/src/Atomizer/Models/AtomizerJob.cs \
  /Users/mbuhl/projects/Atomizer/src/Atomizer/Models/ValueObjects/RetryStrategy.cs \
  /Users/mbuhl/projects/Atomizer/src/Atomizer/Exceptions/InvalidQueueKeyException.cs
```
All three files must return at least 5 matches each.
</verification>

<success_criteria>
- All 17 files have `/// <summary>` on every public type and public member
- Enum members in AtomizerJobStatus and MisfirePolicy all have one-line summary docs
- GetEqualityValues overrides in QueueKey/JobKey/LeaseToken/Schedule use `/// <inheritdoc/>`
- RetryStrategy.GetEqualityValues and Fixed/Intervals/Exponential have full multi-line docs
- Exception constructors matched to correct overload-specific summaries
- No `<exception>` tags added anywhere
- No existing code logic changed — only doc comment lines added
</success_criteria>

<output>
After completion, create `.planning/phases/05-cleanup-and-versioning/05-03-SUMMARY.md`
</output>
